server:
  port: 8081
  error:
    include-message: always
  servlet:
      session:
        timeout: 1800s  # 30 minutes
        persistent: true  # Sessions persistantes
        tracking-modes: COOKIE
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/auth
    username: root
    password: Lafraouzi@2002
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

  
